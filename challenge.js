//  const printToDom =(stringToPrint,divId)=> {
//    const selectdDiv =document.getElementById (divId)
//    selectdDiv.innerHTML += stringToPrint;
//    };


//print the band name
//deal with the counter

const takeNumber = (bandName,'band');

const bandstring = `<h3>${bandNumber}. ${bandName}<h/3>` bandNumber++
printToDom =(stringToPrint,divId)=> {

}

takeNumber("Galactic Scum") // This should display "1. Galactic Scum" in the DOM
takeNumber("Underdogs") // This should display "2. Underdogs" in the DOM

